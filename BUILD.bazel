load("@npm_bazel_typescript//:defs.bzl", "ts_config", "ts_library")

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
    deps = [],
    visibility = ["//visibility:public"],
)

ts_library(
    name = "ts",
    srcs = glob(["*.ts"]),
    deps = [],
    tsconfig = "//:tsconfig"
)
